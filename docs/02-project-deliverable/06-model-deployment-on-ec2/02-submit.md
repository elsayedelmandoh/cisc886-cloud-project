# what to submit

## Section 6 - Model Deployment on EC2 (3 marks)

### Report Content

1. **EC2 instance configuration table** documenting:
   - Instance name
   - Instance type (g4dn.xlarge)
   - AMI used
   - Region
   - Storage size

2. **Installation commands** copy-pasted verbatim showing:
   - SSH connection to EC2
   - Ollama installation
   - Model download from S3
   - Modelfile creation
   - Model import to Ollama

3. **Terminal screenshot** showing:
   - Ollama serving the fine-tuned model
   - Model name visible in output

4. **API curl test** with:
   - Exact curl command used
   - Screenshot showing the response from the model

> All commands must be copy-pasted verbatim in both the report and the README.
